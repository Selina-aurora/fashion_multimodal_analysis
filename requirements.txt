# Runtime dependencies for the current 3.1 vision pipeline.
# For CUDA, install the PyTorch build that matches the server CUDA runtime first.
torch>=2.1
transformers>=4.46,<5
numpy>=1.26
opencv-python>=4.8
Pillow>=10.0
tqdm>=4.66
