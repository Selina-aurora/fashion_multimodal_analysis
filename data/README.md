# Local Data

Raw DeepFashion2 data is intentionally not committed. The current scripts expect the main dataset in a sibling `fashion_data/` directory as documented in the root README. Small local smoke-test images may be placed here when needed.
